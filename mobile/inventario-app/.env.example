# Desarrollo local (mismo contenedor web, puerto 8701 → FastAPI interno 8001)
EXPO_PUBLIC_API_URL=http://localhost:8701/api/v1

# Producción / staging (mismo dominio, nginx enruta /api/v1 al puerto 8001 del contenedor web)
# EXPO_PUBLIC_API_URL=https://inventarios.almacen.proyectosceib.com.mx/api/v1
